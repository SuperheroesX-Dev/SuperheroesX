package com.sx_dev.sx.util.handlers;

//import net.minecraftforge.common.util.EnumHelper;

public class EnumHandler {

    //public static final EnumAction THROW = EnumAction.addEnum(EnumAction.class, "THROW", null, (Object) null);

    public enum ParticleType {
        DEFAULT
    }

    public enum HUDPositions {
        TOP_LEFT,
        TOP_CENTER,
        TOP_RIGHT,
        LEFT,
        RIGHT,
        BOTTOM_LEFT,
        BOTTOM_RIGHT
    }
}